<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/uitm"
        android:label="@string/app_name"
        android:roundIcon="@drawable/uitm"
        android:supportsRtl="true"
        android:theme="@style/Theme._1_ElectricityBill"
        tools:targetApi="31">

        <!-- SplashActivity as LAUNCHER -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:label="Electricity Bill">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Electricity Bill Calculator" />

        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:label="Bill History" />

        <activity
            android:name=".DetailActivity"
            android:exported="false"
            android:label="Bill Details" />

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:label="About" />

    </application>
</manifest>